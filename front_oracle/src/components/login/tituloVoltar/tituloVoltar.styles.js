import { StyleSheet } from "react-native";

const stylesTituloVoltar = StyleSheet.create({
    formContainer: {
        paddingVertical: 8,
        paddingHorizontal: 30,
        flexDirection: "row",
        justifyContent: "flex-end",
    },
    titleText: {
        fontSize: 20,
        marginTop: 50,
        marginBottom: 5,
        marginRight: "auto",
        color: "#C74634",
    },
});

export default stylesTituloVoltar;