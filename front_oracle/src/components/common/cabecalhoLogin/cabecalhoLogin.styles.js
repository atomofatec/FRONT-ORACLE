import { StyleSheet } from "react-native";
import * as Styles from "../../../styles/index";

const stylesCabecalhoLogin = StyleSheet.create({
    logo: {
        width: '100%',
        height: 100,
        marginTop: 50,
        marginBottom: 30,
    },
});

export default stylesCabecalhoLogin;