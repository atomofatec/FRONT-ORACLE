import { StyleSheet } from "react-native";

const stylesButtonSmall = StyleSheet.create({
    button: {
        backgroundColor: "#C74634",
        paddingVertical: 8,
        paddingHorizontal: 20,
        borderRadius: 10,
        marginBottom: 30,
    },
    buttonText: {
        color: "#fff",
        textAlign: "center",
        fontSize: 18,
        fontWeight: "bold",
    },
});

export default stylesButtonSmall;