import { StyleSheet } from "react-native";

const stylesTitle = StyleSheet.create({
    title: {
        fontSize: 20,
        marginTop: 50,
        marginBottom: 50,
        color: "#C74634",
    },
});

export default stylesTitle;