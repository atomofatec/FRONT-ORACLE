import { StyleSheet } from "react-native";
import * as Styles from "../../../styles/index";

const stylesTitleDash = StyleSheet.create({
    title: {
        fontSize: 18,
        margin: 10,
        color: Styles.colors.cinzaEscuro,
    },
});

export default stylesTitleDash;